import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Обо мне",
  description: "Пентестер и веб-разработчик — Астана, Казахстан.",
};

export default function AboutPage() {
  return (
    <section className="mx-auto max-w-2xl px-6 py-16">
      <h1 className="text-3xl font-semibold tracking-tight text-foreground">
        Обо мне
      </h1>
      <div className="mt-6 space-y-5 leading-relaxed text-muted">
        <p>
          3.5 года практического пентеста (OSCP) — до того, как начал писать
          продакшн-фронтенд. Это меняет то, на что смотришь при разработке:
          не «работает ли», а «что произойдёт, если это специально сломать».
        </p>
        <p>
          Практически это значит: авторизация проверяется там, где реально
          происходит доступ к данным, а не только в роутинге. Внешний ввод
          не доверяется по умолчанию. Ошибки не текут наружу деталями стека.
          Это не чек-лист «для галочки» — так action и должен был выглядеть
          с самого начала.
        </p>
        <p>
          Сейчас строю студию веб-разработки в Астане — с этим же подходом:
          безопасность не продаётся отдельной опцией, она часть того, что
          сдаётся клиенту по умолчанию.
        </p>
        <p>
          На странице{" "}
          <a href="/projects" className="text-primary hover:underline">
            проектов
          </a>{" "}
          — один рабочий проект и три концепт-кейса, каждый честно помечен,
          какой есть какой.
        </p>
      </div>
    </section>
  );
}
